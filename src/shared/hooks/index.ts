/**
 * Shared generic hooks.
 * Only truly reusable, business-agnostic hooks belong here.
 * Feature/area-specific hooks must stay in their respective area (admin/ or portal/).
 */
export { useDebounce } from './useDebounce';
